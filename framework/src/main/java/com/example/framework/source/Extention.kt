package com.example.framework.source

class Extensition {
/*
    fun <E, D> RealmResults<D>.convert(): ArrayList<E> where D : RealmModel, D : RealmMapper<E, D> {
        return RealmMapper.map(this)!!
    }

    fun <E, D> RealmList<D>.convert(): ArrayList<E>? where D : RealmModel, D : RealmMapper<E, D> {
        return RealmMapper.map(this)
    }

    fun <E, D> List<D>.convert(): ArrayList<E> where D : RealmModel, D : RealmMapper<E, D> {
        return RealmMapper.map(this)!!
    }

    fun <E, D> List<E>.convert(clazz: Class<D>): RealmList<D> where D : RealmModel, D : RealmMapper<E, D> {
        return RealmMapper.map(clazz, this)!!
    }

    fun <E, D> E.convert(clazz: Class<D>): D where D : RealmModel, D : RealmMapper<E, D> {
        return RealmMapper.map(clazz.name, this)
    }

    fun <E, D> D.convert(): E where D : RealmModel, D : RealmMapper<E, D> {
        return RealmMapper.map(this)
    }
*/

}